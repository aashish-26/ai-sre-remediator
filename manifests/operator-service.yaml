apiVersion: v1
kind: Service
metadata:
  name: ai-operator-svc
  namespace: default
  labels:
    app: ai-operator
spec:
  selector:
    app: ai-operator
  ports:
  - name: metrics
    port: 9100
    targetPort: 9100
  - name: webhook
    port: 5001
    targetPort: 5001
