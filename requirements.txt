kopf>=1.38.0
kubernetes>=26.0.0
requests>=2.31.0
pydantic>=2.5.0    # for schema validation
jsonschema>=4.19.0
flask>=2.3.0       # small web UI (optional)
prometheus-client>=0.16.0
python-dotenv>=1.0.0
pytest>=7.4.0

# Additional dependencies inferred from README and typical operator tooling
pyyaml>=6.0                 # YAML parsing for templates and manifests
jinja2>=3.1.2               # template rendering for remediation templates
gitpython>=3.1.30          # commit audit entries to repo (optional GitOps flow)
cryptography>=41.0.0       # signing/verifying templates and signatures
pyjwt>=2.8.0               # JWT support if using signed tokens
aiohttp>=3.8.5             # async HTTP client (MCP calls, optional)
tenacity>=8.2.0            # retry/backoff helper for transient calls
click>=8.1.3               # CLI helpers (optional)
structlog>=23.1.0          # structured logging
typing-extensions>=4.6.0   # typing backports
pytest-asyncio>=0.22.0     # async test support

# Optional / tooling
coverage>=7.2.0            # test coverage reporting (dev)
ruff>=0.4.0                # optional linter (dev)

# NOTE: pinned versions are conservative minimums; adjust pins as needed.
